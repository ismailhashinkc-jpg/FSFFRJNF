services:
  - type: web
    name: flask-app
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --config gunicorn.conf.py app:app
    envVars:
      - key: SECRET_KEY
        value: your-very-secure-secret-key-here  # Generate a strong one
